using System;
using System.Collections.Generic;
using System.Data.Entity;
using System.Linq;
using System.Web;

namespace IndividuellOppgave4.Models
{
    /*
    public class DBInit : DropCreateDatabaseAlways<DBContxt>
    {
        protected override void Seed(DBContxt context)
        {
            var nyQuestion1 = new FAQuestions()
            {
                Title = "Order",
                Question = "My order is taking long time",
                Answer = "Generally orders take 2-3 days and in some cases it can take 5 days. Wait until 5 days and contact us"
            };
            var questionList = new List<FAQuestions>();
            questionList.Add(nyQuestion1);
            context.FAQuestions.AddRange(questionList);
            base.Seed(context);

        }
    } */
}